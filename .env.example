# ─────────────────────────────────────────────────────────────
# Mini Legal Graph — exemplo de variáveis de ambiente
# Copie para .env e ajuste. NUNCA versione o .env real.
# ─────────────────────────────────────────────────────────────

# Banco de dados (Postgres + pgvector)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mini_langgraph_legal
# Dentro do docker-compose o host é "postgres"; localmente use "localhost"
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mini_langgraph_legal

# API
API_PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Autenticação
JWT_ACCESS_SECRET=troque-por-um-segredo-forte-access
JWT_REFRESH_SECRET=troque-por-um-segredo-forte-refresh
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=604800

# Provider de IA — sempre Hugging Face (sem fallback fake).
# HF_API_TOKEN é OBRIGATÓRIO: sem ele a aplicação não sobe.
# O seed indexa os documentos com o MESMO modelo de embeddings do runtime;
# se trocar HF_EMBEDDINGS_MODEL, rode o seed de novo (npm run db:seed) para
# reindexar, senão a busca vetorial não encontra nada.
HF_API_TOKEN=
HF_MODEL=MiniMaxAI/MiniMax-M3
HF_EMBEDDINGS_MODEL=sentence-transformers/all-MiniLM-L6-v2
HF_TIMEOUT_MS=30000

# Retrieval
RETRIEVAL_TOP_K=5
RETRIEVAL_THRESHOLD=0.35

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001
